"""
Initial columns for column generation.

One-item-per-bin strategy: for each item, create a pattern with only that item.
"""


def initial_columns(n_items):
    """Generate initial columns for the bin packing RMP.

    Returns:
        List of lists, e.g. for 3 items: [[0], [1], [2]]
    """
    # EXERCISE 0: Return [[0], [1], ..., [n_items-1]]

    raise NotImplementedError("Exercise 0: Generate initial columns.")
